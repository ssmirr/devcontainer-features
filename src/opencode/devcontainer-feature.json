{
  "id": "opencode",
  "version": "2.1.0",
  "name": "OpenCode",
  "description": "Installs OpenCode - the open source coding agent. Mounts your host config (plugins, themes, API keys) into the container. Run 'opencode-web' to start the web UI, 'opencode-stop' to stop it.",
  "documentationURL": "https://opencode.ai/docs",
  "options": {
    "version": {
      "type": "string",
      "default": "latest",
      "description": "OpenCode version to install (e.g. '1.2.27' or 'latest')"
    },
    "permission": {
      "type": "string",
      "default": "allow",
      "description": "Default permission level for all actions",
      "enum": ["allow", "ask", "deny"]
    }
  },
  "mounts": [
    {
      "source": "${localEnv:HOME}${localEnv:USERPROFILE}/.config/opencode",
      "target": "/usr/local/share/opencode/host-config",
      "type": "bind"
    }
  ],
  "installsAfter": ["ghcr.io/devcontainers/features/common-utils"]
}
